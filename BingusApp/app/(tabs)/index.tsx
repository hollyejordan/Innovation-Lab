import React from 'react';
import { registerRootComponent } from 'expo';
import Navigation from './navigation'; // Import the navigation component

registerRootComponent(Navigation); // Register the navigation component